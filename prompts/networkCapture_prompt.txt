Using the rules and conventions defined in skills/SKILL.md, generate a new WebdriverIO test for my iOS app that validates the network capture flow.

Requirements:
- Follow the Page Object Model exactly as defined in the skill.
- Place the Page Object in test/screens and the test in test/specs.
- Use accessibility ID selectors.
- Use explicit waits.
- Assume the test runs on Sauce Labs and use the appropriate network capture and network profile capabilities.
- Name the screen NetworkCaptureScreen.js and the test networkCapture.spec.js.
- Iterate through the list of network profiles documented in skills/SKILL.md and create a capabilities block so that all profiles can be tested in parallel.
- Execute the tests by running the terminal command: npx wdio run networkCapture.conf.js

The test must include tests that validate all of the Network Profiles in the table as defined in skills/SKILL.md.

Output both the Page Object and the test file.